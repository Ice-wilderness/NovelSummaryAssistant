# 上传工作流改造验证记录

验证日期：2026-05-20

## 自动回归

- `python -m unittest discover tests`：通过，84 个测试 OK。
- `npm run build`（frontend）：通过，TypeScript 检查和 Vite 构建均成功。

## 工作流核对

- 小说总结：页面已接入历史项目、项目名称、上传章节文件、默认导出目录、自定义输出目录和打开目录按钮；任务请求传递 `project_slug`、`uploaded_file_ids` 和可选自定义输出目录。
- 文章总结：页面已接入历史项目、项目名称、上传文章文件、默认导出目录、自定义输出目录和打开目录按钮；任务请求使用上传引用并保留上传顺序。
- 自定义总结：页面已接入历史项目、项目名称、上传参考材料、默认导出目录、自定义输出目录和打开目录按钮；任务请求使用上传引用和当前自定义指令。
- 章节分割：页面已接入历史项目、项目名称、单文件上传、默认导出目录、自定义输出目录和打开目录按钮；任务请求使用上传源 TXT 和项目输出目录。

## 后端核对

- 上传文件保存到 `<runtime_base>/workspace/projects/<project_slug>/inputs/`。
- 默认导出目录解析到 `<runtime_base>/exports/<project_slug>/<workflow>/`。
- 自定义输出目录覆盖默认导出目录。
- 历史项目 API 返回上传文件、输出目标、最近任务状态和缺失文件警告。
- 打开目录 API 对托管目录会创建后打开，对不存在的自定义目录返回清晰错误。

## 清理核对

- 代码搜索确认不存在 `[PathDropDebug]`、路径拖拽入口、文件路径伪回退逻辑。
- `/api/utils/resolve-path` 已收窄为显式目录校验接口，不再把文件路径转换为上级目录。
