# 雷点扫描功能 — 最终需求文档

> **版本**：v2.0 终稿（已整合全部确认反馈）
> **功能名称**：雷点扫描 / 阅读避雷扫描
> **定位**：NovelSummaryAssistant 的附加功能，与小说总结功能共用项目体系

---

## 一、功能定位

### 一句话定义

用户提前配置自己不能接受的剧情雷点规则；系统基于章节文本进行 AI 扫描，输出每个疑似雷点的 **类型、章节位置、严重程度、触发原因、短证据片段、置信度**，并同时生成三个剧透等级的描述供用户通过滑块自由切换查看。

### 与现有功能的关系

- 雷点扫描 **不是独立项目类型**，而是在现有 `novel_summary` 项目上的附加操作
- 分章完成后的项目（当前 `chapter_split` 类型）也可以使用 **精确模式** 进行雷点扫描
- 项目体系后续将扩展为 **分章 → 总结 → 雷点扫描** 的一条龙工作流

---

## 二、章节拆分架构重构

> [!IMPORTANT]
> 雷点扫描需要逐章精确定位到段落级别。这要求每个文件对应一个章节。因此需要对现有的章节拆分和总结架构进行重构。

### 当前问题

现有架构中拆分粒度和总结粒度耦合——`chapters_per_file=20` 导致小总结的粒度也是 20 章。雷点扫描无法在多章合并文件中精确定位。

### 重构方案

#### 拆分层改动

- **移除 `chapters_per_file` 参数**，章节拆分固定为每章一个文件
- 前端 `SplitterPage` 移除该配置项，简化用户认知
- 输出固定为 `第001章.txt`、`第002章.txt` 等单章文件

#### 总结层改动

- **新增 `summary_batch_size` 参数**，在总结阶段控制合并粒度
- 前端 `NovelSummaryPage` 新增配置项"小总结合并章节数"
- `summary_batch_size=1`：逐章总结（精细，token 多）
- `summary_batch_size=5`：每 5 章合并为一个小总结（经济，token 少）
- 后端 `summarization_stages.py` 的 `run_small_summary_stage` 支持将多个单章文件合并后发给 AI

#### 架构对比

```
旧架构（拆分粒度 = 总结粒度）：
整本TXT → 拆分(N章/文件) → 小总结(逐文件) → 大总结 → ...

新架构（拆分粒度 与 总结粒度 解耦）：
整本TXT → 拆分(固定1章/文件) → 小总结(每 summary_batch_size 章合并) → 大总结 → ...
                                    ↘
                                  雷点扫描（逐章精扫，段落编号稳定）
```

#### 旧项目迁移方案

当用户打开一个旧项目（多章合并文件）时：

1. **自动检测**：系统检测项目文件是否为多章合并格式（如 `第1章-第20章.txt`）
2. **提示用户**：弹出提示说明需要重新拆分以适配新架构
3. **自动重拆**：将多章合并文件重新拆分为单章文件
4. **迁移粒度**：将原来的 `chapters_per_file` 值（如 20）自动设为该项目的 `summary_batch_size`，保留用户原来的总结粒度意图

---

## 三、产品策略：单一原文精扫

### 扫描策略

| 策略 | 输入 | 前置条件 | 调节项 |
| --- | --- | --- | --- |
| **原文精扫** | 直接扫描所选章节原文 | 仅需分好章节（1章/文件） | 通过 `precise_chapter_batch_size` 控制单次精扫章节批量 |

### 前置条件检测逻辑

```
用户选择项目 → 进入"雷点扫描" Tab
                    │
                    ├─ 检查是否为单章文件
                    │
                    ├─ 检查扫描范围、雷点档案、API 配置
                    │
                    └─ 通过后直接进入原文精扫
```

> [!IMPORTANT]
> **"仅小总结"模式**：作为小说总结流程的独立准备能力保留，可只生成小总结并停止；雷点扫描不再依赖小总结覆盖率。

### 扫描范围处理

此检测在 **用户点击开始扫描前** 进行：

- 用户指定扫描范围为第 50-200 章时，系统直接对该范围内所有可读单章文件进行原文精扫。
- 若范围内没有可扫描章节，或项目仍是旧版多章合并文件，则提示用户调整范围或先迁移章节粒度。

---

## 四、核心用户场景

### 场景 1：读前避雷

用户导入一本长篇小说，先完成章节拆分和小总结，选择自己的雷点档案，点击扫描。系统输出：

- 哪些章节可能踩雷
- 雷点大概发生在什么段落
- 是否是主线情节、是否可以跳过
- 三个剧透等级的描述，用户通过滑块按需查看

### 场景 2：追更读者扫描新章节

用户之前扫过前 300 章，现在新增 301-320 章，只想扫描新增章节。

- 支持 **章节级断点续扫**
- 支持只扫描未处理章节
- 新结果与历史报告自动合并
- 从中断点继续，不重复已扫章节

### 场景 3：不同雷点档案

同一本书，用户可以用不同档案扫描：

- 档案 A：高敏感（虐主 + NTR + 角色死亡 + 血腥）
- 档案 B：仅感情线（NTR + 虐恋 + 后宫）
- 档案 C：轻度预警（只看主角死亡）

每次扫描的结果独立保存为一条历史记录。

---

## 五、数据模型设计

### 5.1 雷点档案（TriggerProfile）

**存储位置**：全局存储，独立于项目。路径：`workspace/trigger_profiles/`。

```json
{
  "id": "profile_001",
  "name": "高敏感避雷档案",
  "description": "包含所有严重雷点的全面扫描",
  "created_at": 1716300000,
  "updated_at": 1716300000,
  "rule_groups": [
    {
      "id": "group_emotion",
      "name": "感情类",
      "rules": ["rule_ntr", "rule_abuse_romance", "rule_harem"]
    },
    {
      "id": "group_character",
      "name": "角色类",
      "rules": ["rule_character_death", "rule_character_blacken", "rule_protagonist_stupid"]
    }
  ],
  "rules": [ "...见下方 TriggerRule 结构" ]
}
```

用户可 **新增、删除、重命名** 规则分组，分组仅用于 UI 展示归类。

### 5.2 雷点规则（TriggerRule）

```json
{
  "id": "rule_abuse_romance",
  "name": "感情线虐恋",
  "group_id": "group_emotion",
  "description": "主角或重要角色之间出现长期精神虐待、强迫关系、PUA、冷暴力等剧情",
  "matching_policy": "explicit_or_strongly_implied",
  "severity_threshold": 2,
  "enabled": true,
  "examples": [
    "一方长期操控另一方的社交关系",
    "以爱为名限制自由",
    "持续羞辱或贬低伴侣"
  ],
  "negative_examples": [
    "普通争吵后和解",
    "短暂误会但无长期伤害"
  ]
}
```

| 字段  | 说明  |
| --- | --- |
| `matching_policy` | `explicit_only`（仅明确出现）/ `explicit_or_strongly_implied`（明确或强暗示）/ `any_hint`（轻微迹象也提示） |
| `severity_threshold` | 严重程度阈值（1-5），低于此值的命中不纳入正式结果 |
| `examples` | 正例，帮助 AI 理解什么算命中 |
| `negative_examples` | 反例，**优先级高于正例**，帮助 AI 排除误判 |

### 5.3 内置模板规则（分组）

用户可修改、删除、新增分组和规则：

| 分组  | 规则  |
| --- | --- |
| **感情类** | NTR / 感情背叛、感情线虐恋、后宫 / 多角关系、亲密关系操控 |
| **角色类** | 主要角色死亡、角色黑化、主角降智 |
| **暴力类** | 血腥酷刑、性暴力、动物伤害 |
| **剧情类** | 背叛、精神控制、烂尾 / 开放式 BE、重大误会长期不解 |
| **敏感类** | 未成年人相关敏感剧情、强迫关系 |

### 5.4 扫描结果（ScanFinding）

每条 finding 同时包含三个剧透等级的描述：

```json
{
  "finding_id": "f_001",
  "rule_id": "rule_character_death",
  "rule_name": "主要角色死亡",
  "chapter_file": "第089章.txt",
  "chapter_title": "第八十九章 落幕",
  "paragraph_ids": ["P034", "P035"],
  "severity": 4,
  "confidence": 0.86,
  "is_main_plot": true,
  "review_status": "unreviewed",
  "user_note": "",
  "in_skip_list": false,

  "spoiler_levels": {
    "low": {
      "description": "本章存在主要角色死亡相关内容",
      "skip_advice": "建议谨慎阅读"
    },
    "standard": {
      "description": "一名重要配角在战斗中死亡，对主角产生重大影响",
      "skip_advice": "不建议跳过，属于主线转折"
    },
    "detailed": {
      "description": "林师姐在第三次攻城战中为掩护主角撤退而牺牲，主角因此性情大变，开始走向复仇之路",
      "evidence_quote": "「她的身体缓缓倒下，手中的剑深深插入城墙的裂缝里……」",
      "skip_advice": "不建议直接跳过，属于主线转折，影响后续 10+ 章剧情走向"
    }
  }
}
```

**`evidence_quote` 展示规则**：

| 剧透等级 | 是否展示 | 长度限制 |
| --- | --- | --- |
| 简略  | ❌   | —   |
| 标准  | ❌   | —   |
| 详细  | ✅   | ≤ 80 字 |

**`review_status` 可选值**：`unreviewed`（未复核）/ `confirmed`（已确认）/ `false_positive`（误报）

### 5.5 扫描事件（ScanEvent）— AI 聚合后的结构

```json
{
  "event_id": "evt_001",
  "rule_id": "rule_character_death",
  "rule_name": "主要角色死亡",
  "first_chapter": "第089章.txt",
  "related_chapters": ["第089章.txt", "第090章.txt", "第091章.txt"],
  "max_severity": 5,
  "max_confidence": 0.92,
  "is_main_plot": true,
  "finding_ids": ["f_001", "f_002", "f_003"],

  "event_summary": {
    "low": "多个章节涉及同一角色死亡事件",
    "standard": "一名重要配角在战斗中死亡，后续多章描写影响",
    "detailed": "林师姐在第89章牺牲，第90-91章描写主角的崩溃与黑化转变过程"
  }
}
```

### 5.6 扫描报告（ScanReport）

```json
{
  "report_id": "rpt_20260521_001",
  "project_slug": "my-novel",
  "profile_id": "profile_001",
  "profile_name": "高敏感避雷档案",
  "scan_mode": "precise",
  "scan_range": { "start": 1, "end": 200 },
  "scan_config": {
    "min_confidence": 0.65,
    "keep_low_confidence": true,
    "verification_enabled": true,
    "verification_api_id": "api_002",
    "precise_chapter_batch_size": 5
  },
  "created_at": 1716300000,
  "completed_at": 1716303600,
  "status": "completed",

  "summary": {
    "total_findings": 23,
    "verified_findings": 18,
    "pending_review": 5,
    "rules_hit": [
      { "rule_id": "rule_character_death", "count": 2, "max_severity": 5 },
      { "rule_id": "rule_abuse_romance", "count": 7, "max_severity": 4 }
    ]
  },

  "events": ["...ScanEvent 数组"],
  "findings": ["...ScanFinding 数组"]
}
```

**存储位置**：项目输出目录下 `exports/{slug}/trigger_scan/`。

---

## 六、扫描工作流设计

### 总体流程

```
┌──────────────────────────────────────────────────────────────────┐
│                          原文精扫流程                              │
│                                                                  │
│  Step 0         Step 2               Step 3(可选)    Step 4      │
│  文本预处理 → 精扫(原文,全部章节) →  二次验证     → AI聚合去重    │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### Step 0：文本预处理

对每章文件做规范化处理：

- 识别章节名
- 分段并给段落编号（`[P001]`、`[P002]`...）
- 长章节切 chunk（每个 chunk 附带稳定定位信息）

**段落切分模块独立化**：段落切分逻辑独立为一个模块，切分结果缓存在 `.summarizer_cache/paragraph_index/` 下，确保同一章节多次扫描的段落编号一致。

由于拆分架构重构后每个文件固定为单章，段落编号天然稳定。

给模型的文本包装格式：

```text
[CHAPTER_FILE] 第012章.txt
[CHAPTER_TITLE] 第十二章 雨夜
[P001] ......
[P002] ......
[P003] ......
```

要求模型返回 `paragraph_ids`，禁止使用"在中间部分"等模糊描述。

### Step 2：精扫（章节级扫描）

**输入**：所有待扫描章节原文。

每章或每个 chunk 调一次模型，输出严格 JSON：

```json
{
  "findings": [
    {
      "rule_id": "rule_character_death",
      "rule_name": "主要角色死亡",
      "severity": 4,
      "confidence": 0.86,
      "paragraph_ids": ["P034", "P035"],
      "is_main_plot": true,
      "spoiler_levels": {
        "low": { "description": "...", "skip_advice": "..." },
        "standard": { "description": "...", "skip_advice": "..." },
        "detailed": { "description": "...", "evidence_quote": "...", "skip_advice": "..." }
      }
    }
  ]
}
```

**强制规则**：

1. 没有证据不得命中
2. 不允许根据标题臆测
3. `evidence_quote` 必须来自原文，≤ 80 字
4. 找不到原文片段则 `confidence` 不得高于 0.6
5. `paragraph_ids` 必须使用预处理阶段分配的编号
6. 用户的 `negative_examples` 优先级高于 `examples`
7. 不得扩大解释雷点规则

### Step 3：二次验证（可选）

**开关**：用户可在扫描配置中开启或关闭，用于避免 AI 幻觉或更换 API 交叉验证。

**API 选择**：支持选择与精扫 **不同的 API** 进行二次验证。

**批量验证**：同一章节的所有 findings 合并在一次 API 调用中验证。

**输入**：原雷点规则 + 原章节相关段落（根据 `paragraph_ids` 提取上下文）+ 第一轮 findings

**输出**：

```json
{
  "verifications": [
    {
      "finding_id": "f_001",
      "verified": true,
      "adjusted_confidence": 0.91,
      "false_positive_reason": "",
      "adjusted_severity": 4
    },
    {
      "finding_id": "f_002",
      "verified": false,
      "adjusted_confidence": 0.25,
      "false_positive_reason": "原文描述的是战斗受伤，并非酷刑折磨",
      "adjusted_severity": null
    }
  ]
}
```

### Step 4：AI 辅助聚合去重

**规则**：

1. 同一章节、同一规则、相邻段落 → 自动合并为一条 finding
2. 跨章节的同一事件 → AI 辅助判断并聚合为 ScanEvent
3. 保留首次出现位置，标记所有关联章节
4. 为每个事件生成三档剧透等级的事件摘要

**AI 聚合输入**：所有通过验证的 findings

**AI 聚合输出**：

```json
{
  "events": [
    {
      "event_id": "evt_001",
      "finding_ids": ["f_001", "f_002", "f_003"],
      "event_summary": {
        "low": "多个章节涉及同一角色死亡事件",
        "standard": "一名重要配角在战斗中死亡，后续多章描写影响",
        "detailed": "林师姐在第89章牺牲，第90-91章描写主角的崩溃与黑化转变过程"
      }
    }
  ],
  "standalone_findings": ["f_010", "f_015"]
}
```

**结果层级**：

```
Report（最终报告）
├── Events（聚合后的雷点事件）
│   ├── Event 1 → [Finding A, Finding B, Finding C]
│   └── Event 2 → [Finding D, Finding E]
└── Standalone Findings（未归入事件的独立命中）
    ├── Finding X
    └── Finding Y
```

---

## 七、断点续扫设计

| 项目  | 决策  |
| --- | --- |
| 续扫粒度 | 章节级，扫完一章即保存状态 |
| 状态管理 | 参考现有 `StateManager` 设计，保存在 `.summarizer_cache/scan_state_{task_id}.json` |
| 中断恢复 | 用户取消扫描后重启，从上次中断的章节继续 |
| 规则变更 | 修改雷点规则后续扫，之前的结果 **不自动失效**，但 UI 显示提示"雷点规则已变更，部分结果可能不适用"，提供 **[全部重扫]** 按钮 |

---

## 八、关键交互设计

### 导航结构

采用 **单入口 + 内部 Tab 切换** 的设计。

```
侧边栏导航：
├─ 小说总结
├─ 文章总结
├─ 自定义总结
├─ 章节分割
├─ 雷点扫描    ← 新增一级入口
├─ 提示词
└─ API 配置
```

雷点扫描页面内部包含三个 Tab：

```
┌──────────────────────────────────────────────────────────┐
│  雷点扫描                                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                 │
│  │ 档案管理  │ │ 扫描配置  │ │ 扫描结果  │                 │
│  └──────────┘ └──────────┘ └──────────┘                 │
│                                                          │
│  [ 当前 Tab 的内容区域 ]                                   │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

### Tab 1：档案管理

左侧档案列表 + 右侧规则编辑器的双栏布局。

```
┌──────────────────────────────────────────────────────────────┐
│  档案列表            │  规则编辑器                              │
│  ┌────────────────┐ │  ┌──────────────────────────────────┐  │
│  │ 🟢 高敏感避雷   │ │  │ 档案名称：高敏感避雷档案            │  │
│  │   ○ 轻度预警    │ │  │ 描述：包含所有严重雷点...            │  │
│  │   ○ 仅感情线    │ │  │                                    │  │
│  │                 │ │  │ ─── 感情类 ──────── [+规则] [×删除] │  │
│  │                 │ │  │ ☑ NTR / 感情背叛                   │  │
│  │  [+ 新建档案]   │ │  │ ☑ 感情线虐恋          [编辑] [删除] │  │
│  │                 │ │  │ ☐ 后宫 / 多角关系                   │  │
│  │  [从模板创建]   │ │  │                                    │  │
│  │                 │ │  │ ─── 角色类 ──────── [+规则] [×删除] │  │
│  │                 │ │  │ ☑ 主要角色死亡                      │  │
│  │                 │ │  │ ☑ 角色黑化                          │  │
│  │                 │ │  │                                    │  │
│  │                 │ │  │ [+ 新建分组]                        │  │
│  └────────────────┘ │  └──────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

**单条规则编辑区**（点击 [编辑] 后展开或弹出）：

| 字段  | 控件类型 | 说明  |
| --- | --- | --- |
| 雷点名称 | TextInput | 规则名称 |
| 说明  | TextArea | 详细描述什么算命中 |
| 严重程度阈值 | NumberInput (1-5) | 低于此值不纳入正式结果 |
| 匹配策略 | Select | 仅明确出现 / 明确或强暗示 / 轻微迹象也提示 |
| 正例  | TextArea (多行) | 每行一个正例 |
| 反例  | TextArea (多行) | 每行一个反例，优先级高于正例 |
| 是否启用 | ToggleSwitch | 临时禁用而不删除 |

---

### Tab 2：扫描配置

**核心配置项**：

| 配置项 | 控件类型 | 默认值 | 说明  |
| --- | --- | --- | --- |
| 项目选择 | Select | 上次使用 | 从已有项目（主要是 novel_summary，精确模式可选 chapter_split）中选择 |
| 扫描档案 | Select | 上次使用 | 从全局档案中选择 |
| 扫描范围 | Range / Select | 全部章节 | 全部 / 新增章节 / 指定区间 |
| 扫描用 API | MultiSelect | 全部启用的 API | 用于原文精扫的 API |
| 最低置信度 | NumberInput | 0.65 | 低于此值的结果进入待复核 |
| 是否保留低置信度项 | ToggleSwitch | 是   | 放入待复核区域 |
| 是否生成跳读建议 | ToggleSwitch | 是   | 产生 skip_advice 字段 |

**高级配置**（折叠区域）：

| 配置项 | 控件类型 | 默认值 | 说明  |
| --- | --- | --- | --- |
| 精扫批量大小 | NumberInput | 5   | 每批发送几个原文章节 |
| 开启二次验证 | ToggleSwitch | 开启 | 可选开关 |
| 验证用 API | Select | 与扫描相同 | 支持选择不同 API 做交叉验证 |
| 最大证据摘录长度 | NumberInput | 80  | evidence_quote 的字符限制 |

**启动前检查**：

点击 [开始扫描] 后，系统在启动前依次检查：

1. **旧项目合并检测**：检测项目是否使用旧版多章合并格式，若是则提示重拆并自动迁移粒度
2. **章节范围检查**：确认扫描范围内存在可读单章文件
3. **规则变更提醒**：如果存在中断的历史扫描且规则已修改，提示用户

全部通过后正式启动扫描任务。

---

### Tab 3：扫描结果

**顶部控制区**：

```
┌──────────────────────────────────────────────────────────────┐
│  扫描历史：[ 2026-05-21 高敏感档案 (全书) ▼ ]                  │
│                                                              │
│  剧透等级：  ○──────●──────○                                  │
│             简略    标准    详细      ← 全局滑块              │
│                                                              │
│  筛选：[雷点类型▼] [严重程度▼] [置信度▼] [章节范围▼]            │
│        [复核状态▼] [☐只看主线] [☐只看高风险]                   │
│                                                              │
│  视图：[事件视图✓] [逐条视图]    导出：[MD] [JSON]             │
└──────────────────────────────────────────────────────────────┘
```

**扫描历史下拉**：列出该项目的所有历史扫描记录，格式为 `日期 + 档案名 + 扫描范围`，支持查看/删除历史报告。

**剧透等级滑块**：

- **全局滑块**：从左到右依次为 **简略 → 标准 → 详细**
- 切换时所有结果的描述内容同步变化，无需重新请求 API
- **单条滑块**：每条结果也有独立的小滑块，可单独调整该条的剧透等级，覆盖全局设置

**事件视图（默认）**：

展示 AI 聚合后的 ScanEvent，每个事件可展开查看包含的 findings。

```
┌──────────────────────────────────────────────────────────────┐
│  🔴 事件：主要角色死亡                                        │
│  章节：第89-91章 │ 最高严重度：5 │ 置信度：0.92 │ 主线          │
│  一名重要配角在战斗中死亡，后续多章描写影响    ← 标准等级描述    │
│                                                              │
│  剧透：○──●──○   [展开详情 ▼]   [标记已确认] [加入跳读清单]   │
│         简 标 详                                              │
│  ┌─ 展开后 ─────────────────────────────────────────────┐    │
│  │ Finding 1: 第89章 P034-P035  置信度:0.92  [查看上下文] │    │
│  │ Finding 2: 第90章 P012-P015  置信度:0.85  [查看上下文] │    │
│  │ Finding 3: 第91章 P003-P008  置信度:0.78  [查看上下文] │    │
│  └────────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────┤
│  🟡 事件：感情线虐恋                                          │
│  章节：第32-38章 │ 最高严重度：4 │ 置信度：0.81 │ 主线          │
│  存在情感操控相关剧情                          ← 标准等级描述   │
│                                                              │
│  剧透：○──●──○   [展开详情 ▼]   [标记误报] [添加备注]         │
└──────────────────────────────────────────────────────────────┘
```

**逐条视图**：

平铺展示所有 findings（不按事件分组），以可筛选表格形式呈现：

| 雷点类型 | 章节  | 段落  | 严重度 | 置信度 | 描述  | 主线  | 复核状态 | 操作  |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 主要角色死亡 | 第89章 | P034-P035 | 5   | 0.92 | (根据当前剧透等级显示) | 是   | 未复核 | [查看上下文] [标记] |
| 感情线虐恋 | 第32章 | P018-P022 | 4   | 0.81 | (根据当前剧透等级显示) | 是   | 已确认 | [查看上下文] [标记] |

**每条结果支持的操作**：

| 操作  | 说明  |
| --- | --- |
| 查看上下文 | 弹出模态框，展示命中段落前后各 2-3 个段落的原文，高亮命中段落 |
| 标记为误报 | 将 `review_status` 改为 `false_positive` |
| 标记为已确认 | 将 `review_status` 改为 `confirmed` |
| 添加备注 | 用户添加自定义备注文本 |
| 加入跳读清单 | 将该章节/段落加入跳读清单 |
| 调整剧透等级 | 单条独立滑块，覆盖全局设置 |

**上下文查看模态框**：

```
┌────────────────────────────────────────────┐
│  第89章 · 第八十九章 落幕                    │
│                                            │
│  [P032] 普通段落文本...                      │
│  [P033] 普通段落文本...                      │
│  ▶ [P034] 命中段落文本（高亮背景）...         │  ← 高亮
│  ▶ [P035] 命中段落文本（高亮背景）...         │  ← 高亮
│  [P036] 普通段落文本...                      │
│  [P037] 普通段落文本...                      │
│                                            │
│  命中规则：主要角色死亡                       │
│  置信度：0.92 │ 严重度：5                    │
│                                            │
│                                   [关闭]    │
└────────────────────────────────────────────┘
```

**待复核区域**：

页面底部或作为筛选项展示低置信度结果。与正式结果区分，使用降低饱和度的样式。

---

### 扫描进度展示

扫描运行过程中，在右侧日志面板（复用现有 `LogPanel` + SSE）实时展示：

- **进度条**：已扫描章节数 / 总章节数
- **阶段指示**：`精扫阶段 [==> ] 20%` → `二次验证 [=====> ] 60%` → `报告写入`
- **实时日志**：当前正在处理的章节、发现的疑似雷点
- **实时结果追加**：每扫完一章，如有发现即追加到结果 Tab 中（不必等全部完成）

---

## 九、提示词设计

### 提示词体系

雷点扫描需要 **三套提示词**，均支持用户在现有的提示词编辑器中自定义编辑：

| 提示词 | 用途  | 使用阶段 |
| --- | --- | --- |
| `trigger_precise_scan` | 精扫系统提示词 | Step 2 |
| `trigger_verification` | 二次验证系统提示词 | Step 3（可选） |
| `trigger_aggregation` | 聚合去重系统提示词 | Step 4 |

> [!IMPORTANT]
> **模块化设计要点**：提示词需要根据用户配置动态组装。例如：
> 
> - 二次验证关闭时，精扫提示词需要指示 AI 同时输出完整 findings（含三档剧透描述）
> - 二次验证开启时，精扫提示词输出首轮 findings，验证提示词负责复核保留或剔除
> - 需要设计好提示词中的可替换占位符（如 `{verification_enabled}` 对应的条件文本）

### 精扫系统提示词核心规则

```text
你是小说剧情雷点扫描助手。
你的任务不是总结全文，而是根据用户提供的雷点规则，寻找文本中明确或强烈暗示的匹配内容。

要求：
1. 只根据给定文本判断，不得使用外部知识。
2. 没有证据不得命中。
3. 必须返回严格 JSON 格式。
4. 每条 finding 必须包含 paragraph_ids（使用预处理分配的编号）。
5. evidence_quote 必须是原文短摘录，不超过 {max_quote_chars} 字。
6. 如果只是弱相关，降低 confidence，不得高于 0.6。
7. 不要扩大解释雷点规则的范围。
8. 用户的 negative_examples 优先级高于 examples。
9. 每条 finding 必须同时提供三个剧透等级的描述：
   - low：仅说明存在某类雷点，不涉及具体剧情
   - standard：简短说明剧情走向，不点明关键细节
   - detailed：完整描述事件经过，包含 evidence_quote
10. 不允许根据章节标题臆测内容。

## 网文术语参考
以下术语在网文语境中的含义可能与字面意义不同：
- 虐主：指主角遭受大量不公正对待、屈辱、痛苦的剧情
- NTR：指角色的伴侣/恋人被他人夺走
- BE：Bad Ending，悲剧结局
- HE：Happy Ending，圆满结局
- 降智：角色智力表现明显低于前期设定
- 开后宫：主角同时与多人保持暧昧或恋爱关系
（用户可在提示词编辑器中扩展此列表）
```

### 用户提示词结构

```text
# 用户雷点规则
{trigger_rules_json}

# 扫描设置
最低置信度：{confidence_threshold}
最大证据摘录长度：{max_quote_chars}
是否生成跳读建议：{generate_skip_advice}

# 待扫描文本
{chapter_text_with_paragraph_ids}

# 输出 JSON Schema
{json_schema}
```

---

## 十、导出报告

### 导出格式

支持两种导出格式：

- `trigger_scan_report.md`（Markdown 报告）
- `trigger_scan_report.json`（结构化数据）

### Markdown 报告模板

```markdown
# 雷点扫描报告

## 扫描配置

- 项目：{project_name}
- 使用档案：{profile_name}
- 扫描模式：{scan_mode}
- 最低置信度：{min_confidence}
- 扫描范围：第 {start} 章 - 第 {end} 章
- 二次验证：{verification_status}
- 扫描时间：{scan_datetime}

## 总览

| 雷点 | 命中次数 | 最高严重度 | 首次出现 |
|------|-------:|--------:|---------|
| 主要角色死亡 | 2 | 5 | 第 89 章 |
| 感情线虐恋 | 7 | 4 | 第 32 章 |

## 雷点事件

### 事件 1：主要角色死亡

- 关联章节：第 89-91 章
- 最高严重度：5
- 最高置信度：0.92
- 是否主线：是

#### 简略描述
多个章节涉及同一角色死亡事件

#### 标准描述
一名重要配角在战斗中死亡，后续多章描写影响

#### 详细描述
林师姐在第89章牺牲，第90-91章描写主角的崩溃与黑化转变过程

**证据片段**：
> 「她的身体缓缓倒下，手中的剑深深插入城墙的裂缝里……」

---

## 待人工复核

| 雷点 | 章节 | 置信度 | 原因 |
|------|------|------:|------|
| 感情背叛 | 第 44 章 | 0.58 | 存在暗示，但证据不足 |

---

> ⚠️ AI 扫描结果仅供辅助参考，不能保证完全发现所有雷点。
```

---

## 十一、跳读清单

### 功能定义

跳读清单是 **独立于扫描结果** 的用户自定义列表。用户在扫描结果中手动将条目加入跳读清单。

### 数据结构

```json
{
  "skip_list_id": "skip_001",
  "project_slug": "my-novel",
  "items": [
    {
      "chapter_file": "第089章.txt",
      "chapter_title": "第八十九章 落幕",
      "paragraph_range": "P034-P047",
      "rule_name": "主要角色死亡",
      "severity": 5,
      "user_note": "角色死亡核心段落",
      "source_finding_id": "f_001"
    }
  ],
  "created_at": 1716300000,
  "updated_at": 1716303600
}
```

### 展示形式

**章节视图**：按章节分组展示跳读建议。

```
┌──────────────────────────────────────────────────┐
│  跳读清单                          [导出清单]      │
├──────────────────────────────────────────────────┤
│                                                  │
│  📖 第89章 · 第八十九章 落幕                       │
│     ├─ P034-P047  主要角色死亡 (严重度5)           │
│     └─ 备注：角色死亡核心段落                      │
│                                                  │
│  📖 第32章 · 第三十二章 暗涌                       │
│     ├─ P018-P022  感情线虐恋 (严重度4)             │
│     └─ 备注：PUA 剧情开始                         │
│                                                  │
│  📖 第44章 · 第四十四章 月下                       │
│     ├─ P008-P015  感情背叛 (严重度3)               │
│     └─ 无备注                                    │
│                                                  │
└──────────────────────────────────────────────────┘
```

### 导出

跳读清单可导出为独立文件（`.md` 格式），方便用户阅读时参照：

```markdown
# 跳读清单 - {project_name}

生成时间：{datetime}
使用档案：{profile_name}

## 第89章 · 第八十九章 落幕
- **跳过段落**：P034-P047
- **雷点**：主要角色死亡（严重度 5）
- **备注**：角色死亡核心段落

## 第32章 · 第三十二章 暗涌
- **跳过段落**：P018-P022
- **雷点**：感情线虐恋（严重度 4）
- **备注**：PUA 剧情开始
```

---

## 十二、产品风险处理

### 1. 漏报风险

必须在 UI 明确提示：

> ⚠️ AI 扫描结果仅供辅助参考，不能保证完全发现所有雷点。

不要让用户以为这是绝对安全过滤器。建议在扫描结果页面顶部固定展示此提示。

### 2. 误报风险

提供完整的反馈闭环：

- 标记误报 → 记录到结果中
- 标记已确认 → 记录到结果中
- 调整规则 → 在档案管理中修改规则的正例/反例
- 重新扫描 → 基于调整后的规则重扫

后续可利用误报数据做本地规则优化（V2）。

### 3. 剧透风险

通过三档剧透等级 + 滑块切换解决。默认为"标准"等级，用户自行决定是否查看更多。

### 4. 敏感内容展示

某些雷点涉及性暴力、自残、虐待等。报告中不应生成过度细节化的描述，只做必要的定位和预警。在 prompt 中明确限制 AI 的描述尺度。

### 5. 原文摘录长度

`evidence_quote` 默认限制 ≤ 80 字。可在高级配置中调整。目的：

- 减少报告中的剧透量
- 减少 token 消耗
- 避免输出大段原文

---

## 十三、技术风险与缓解

### 风险 1：AI 对网文"雷点"定义的理解偏差

**缓解措施**：

- 正例/反例机制（已在规则设计中包含）
- 系统提示词中增加 **网文术语表**（已在 §九 中设计）
- 提供 **规则测试功能**：选一章文本 + 一条规则，查看 AI 的判断结果，方便用户调试规则后再正式扫描

### 风险 2：跨章节聚合的准确性

AI 辅助聚合可能将不相关的 findings 错误归为同一事件。

**缓解措施**：

- 聚合结果允许用户手动拆分/合并
- 提供逐条视图作为备选，用户可绕过聚合结果
- 聚合时给 AI 充足的上下文（各 finding 的描述和章节信息）

---

## 十四、MVP 范围

### MVP（第一版）包含

| 功能  | 说明  |
| --- | --- |
| **雷点档案 CRUD** | 全局存储，支持新建/编辑/删除档案和规则 |
| **规则分组管理** | 内置分组 + 用户自定义分组（新增/删除/重命名） |
| **内置模板规则** | 16 条预设规则，分 5 组，支持正反例 |
| **原文精扫** | 直接扫描所选章节原文，通过批量大小控制速度与 Token 消耗 |
| **仅小总结能力** | 小说总结流程可只生成小总结并停止，不作为雷点扫描前置条件 |
| **章节拆分重构** | 移除 chapters_per_file，总结增加 summary_batch_size |
| **旧项目自动迁移** | 自动检测多章文件，提示并重拆，迁移合并粒度 |
| **三档剧透同时输出** | AI 生成三个等级，UI 全局滑块 + 单条滑块切换 |
| **二次验证** | 可选开启，支持独立验证 API，批量验证以节约 Token |
| **AI 辅助聚合去重** | 跨章节事件聚合，提供事件视图和逐条视图 |
| **上下文查看** | 模态框展示命中段落上下文，高亮命中段落 |
| **跳读清单** | 独立清单，章节视图，支持 Markdown 导出 |
| **结果交互** | 标记误报/已确认/备注 |
| **历史扫描记录** | 多次扫描结果独立保存、查看、下拉列表切换、删除 |
| **章节级断点续扫** | 中断后从上次章节继续，规则变更提供提醒 |
| **提示词可编辑** | 精扫/验证/聚合 三套提示词复用现有编辑器 |
| **实时进度与日志** | SSE 推送状态和章节级中间结果，复用 LogPanel |
| **报告导出** | 导出 MD + JSON 双格式报告 |
| **段落切分缓存** | 独立模块提取，切分结果缓存保证段落 ID 稳定 |

### V2（后续迭代）

| 功能  | 说明  |
| --- | --- |
| Token 成本预估 | 启动扫描前预估 token 消耗 |
| 规则测试功能 | 单章 + 单规则的快速测试工具 |
| 新旧报告对比 | 同一项目不同次扫描的差异对比 |
| 智能规则推荐 | 扫描后建议添加未配置但频繁触发的雷点规则 |
| 智能规则优化 | 基于标记的"误报"反馈自动优化用户的正反例 |
| CSV / HTML 导出 | 提供更多形式的导出报告 |
| 章节时间线视图 | 可视化雷点在整本书中的分布密集度 |

---

## 十五、任务约束总结

| 约束  | 说明  |
| --- | --- |
| **任务并发** | 同一时间只能运行一个任务（总结或扫描），不支持并发 |
| **存储位置** | 扫描结果存放于项目输出目录 `exports/{slug}/trigger_scan/` |
| **档案存储** | 全局存储，路径 `workspace/trigger_profiles/` |
| **缓存路径** | 段落索引 `.summarizer_cache/paragraph_index/` |
| **断点状态** | `.summarizer_cache/scan_state_{task_id}.json` |

---
