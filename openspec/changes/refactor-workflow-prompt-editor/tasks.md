## 1. 后端结构化提示词配置

- [x] 1.1 定义工作流提示词、节点、消息、模块的数据模型和默认工作流映射；验证：新增/更新模型单元测试；完成后提交 git commit
- [x] 1.2 实现从 `DEFAULT_PROMPTS` 与旧 `prompt_cache/*.txt` 初始化结构化配置；验证：覆盖无结构化文件、存在旧文本文件、缺失旧文本文件三种测试；完成后提交 git commit
- [x] 1.3 实现结构化提示词配置的加载、保存、单节点重置和整节点默认值恢复；验证：配置服务测试确认保存后顺序/角色/内容不丢失；完成后提交 git commit
- [x] 1.4 实现提示词模块的创建、更新、删除和引用校验；验证：模块保存测试和“被引用模块不可静默删除”测试；完成后提交 git commit
- [x] 1.5 扩展 `/api/prompts` 相关接口以返回工作流、节点、消息、模块和迁移来源信息；验证：API 路由测试覆盖加载、保存、重置和错误响应；完成后提交 git commit

## 2. 运行时消息渲染

- [x] 2.1 实现结构化消息渲染，支持按节点消息顺序替换变量并展开模块引用；验证：缺失变量、无效模块引用和正常渲染测试；完成后提交 git commit
- [x] 2.2 扩展 LLM 调用路径以接受 chat messages，同时保留旧单文本 `user` 消息兼容；验证：`get_llm_summary_with_config` 和 `call_llm_api` 相关测试通过；完成后提交 git commit
- [x] 2.3 将小说总结和文章总结工作流接入结构化提示词节点；验证：现有工作流服务/逻辑测试通过且默认行为与旧模板等价；完成后提交 git commit

## 3. 前端提示词页面重构

- [x] 3.1 扩展前端 API 类型、客户端和全局状态以支持工作流提示词配置；验证：TypeScript 类型检查通过；完成后提交 git commit
- [x] 3.2 将提示词页面改为按工作流分页，并显示各工作流的提示词节点列表、节点说明和保存状态；验证：前端构建通过并手动确认小说/文章工作流节点可见；完成后提交 git commit
- [x] 3.3 实现节点内消息编辑，支持新增、删除、修改角色、修改内容和上下移动顺序；验证：前端构建通过并手动确认保存后刷新仍保持顺序/角色；完成后提交 git commit
- [ ] 3.4 实现节点级保存和重置操作，并清晰显示未保存状态；验证：保存/重置后后端返回内容与页面状态一致；完成后提交 git commit
- [ ] 3.5 实现提示词模块管理区，支持模块新增、编辑、引用插入和引用关系提示；验证：模块保存后可在节点消息中引用并参与预览/渲染；完成后提交 git commit

## 4. 页面说明引导补全

- [ ] 4.1 新增可复用说明引导组件和字段提示模式，保持现有 WebUI 风格；验证：前端构建通过且响应式布局不重叠；完成后提交 git commit
- [ ] 4.2 为小说总结、文章总结、自定义总结和章节分割页面补全工作流说明、字段含义和按钮用途；验证：页面人工检查所有主要控件有说明；完成后提交 git commit
- [ ] 4.3 为提示词页面补全工作流、节点、消息角色、消息顺序、模块引用和保存/重置行为说明；验证：页面人工检查新提示词功能说明完整；完成后提交 git commit
- [ ] 4.4 为 API 配置和日志区域补全说明，引导用户理解密钥、环境变量、模型获取、启用状态、日志来源和展开行为；验证：页面人工检查说明清晰且不遮挡操作；完成后提交 git commit

## 5. 回归验证与收尾

- [ ] 5.1 运行后端相关测试，优先覆盖配置服务、API 路由、提示词渲染和现有总结逻辑；验证：测试通过或记录无法运行原因；完成后提交 git commit
- [ ] 5.2 运行前端类型检查/构建和可用的 WebUI E2E 测试；验证：检查通过或记录无法运行原因；完成后提交 git commit
- [ ] 5.3 手动验证旧提示词缓存迁移、结构化保存、模块引用、节点重置和任务启动路径；验证：在变更记录中写明验证结果；完成后提交 git commit
