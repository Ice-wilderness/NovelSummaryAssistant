## 1. 后端上传与项目工作区

- [ ] 1.1 定义上传文件、上传批次、项目名称和托管输出目标的数据模型；验证：模型单元测试覆盖项目名清理、上传引用和默认导出路径
- [ ] 1.2 实现上传接口，将文本文件保存到 `<runtime_base>/workspace/projects/<project_slug>/inputs/` 并返回上传引用；验证：API 测试覆盖单文件、多文件、非法类型和大小限制
- [ ] 1.3 实现上传引用解析服务，把上传文件引用转换为后端本地路径；验证：缺失引用、重复引用和顺序保持测试通过
- [ ] 1.4 实现默认导出目录解析，支持 `<runtime_base>/exports/<project_slug>/` 和工作流子目录；验证：路径清理、目录创建和项目名冲突测试通过
- [ ] 1.5 实现历史项目元数据存储，记录项目名、slug、工作流、上传文件、输出目标、最近任务状态和更新时间；验证：创建、更新、缺失文件标记测试通过

## 2. 任务 API 与运行时适配

- [ ] 2.1 扩展小说总结、文章总结、自定义总结和章节分割任务请求，使其可接收上传文件引用和项目输出目标；验证：请求校验测试覆盖缺失上传、缺失项目名和自定义输出目录
- [ ] 2.2 为现有任务 runner 增加适配层，将上传文件组织成当前逻辑需要的源文件或源目录结构；验证：各工作流启动测试确认生成的本地路径可被现有逻辑读取
- [ ] 2.3 实现自定义输出目录覆盖，未选择时使用托管项目导出目录；验证：默认输出和自定义输出两类任务测试通过
- [ ] 2.4 保持现有可恢复任务状态在托管项目目录下可继续使用；验证：重复启动同一项目时缓存路径稳定
- [ ] 2.5 实现历史项目列表和项目详情 API，支持按工作流筛选并返回可恢复状态；验证：API 测试覆盖排序、筛选、缺失项目和缺失文件警告

## 3. 打开目录能力

- [ ] 3.1 实现打开托管导出目录 API，必要时创建目录并调用本地操作系统打开；验证：API 测试覆盖成功打开和目录创建
- [ ] 3.2 实现打开自定义目录 API 或复用同一接口处理自定义路径；验证：无效路径、安全边界和打开失败返回清晰错误
- [ ] 3.3 前端接入打开目录按钮，并在失败时显示错误；验证：人工检查默认导出目录和自定义目录按钮都可见可用

## 4. 前端工作流改造

- [ ] 4.1 新增上传文件控件和上传文件列表组件，支持选择、显示、移除和重新上传；验证：前端构建通过并人工确认多文件顺序保持
- [ ] 4.2 为小说总结、文章总结、自定义总结和章节分割页面接入上传控件、项目名称和输出目录控件；验证：每个页面可选择文件、显示默认导出位置并提交任务
- [ ] 4.3 增加项目名默认派生逻辑，单文件使用文件名，多文件使用时间戳项目名；验证：人工检查默认值可编辑且后端收到清理后的项目名
- [ ] 4.4 保留自定义输出目录浏览按钮，未选择时显示并使用默认导出目录；验证：切换自定义/默认输出后任务请求正确
- [ ] 4.5 增加历史项目选择控件，选择后恢复项目名、上传文件列表、输出目录和可继续任务提示；验证：人工检查未完成项目可快速恢复

## 5. 清理旧路径拖拽代码

- [ ] 5.1 移除文件输入场景中的路径拖拽解析、诊断日志和伪路径回退逻辑；验证：代码搜索不再存在 `[PathDropDebug]` 和文件路径拖拽入口
- [ ] 5.2 删除或收窄 `/api/utils/resolve-path`，只保留自定义输出目录校验所需能力；验证：相关 API 测试更新通过
- [ ] 5.3 更新拖拽路径限制文档，说明新方案不再依赖浏览器暴露真实本地路径；验证：文档与新 UI 行为一致

## 6. 回归验证

- [ ] 6.1 运行后端单元测试和 API 路由测试；验证：上传、任务启动、输出目录和打开目录测试通过
- [ ] 6.2 运行前端类型检查/构建；验证：`npm run build` 通过
- [ ] 6.3 手动验证四个工作流的上传、默认导出、自定义输出和打开目录；验证：记录验证结果并提交
