## 1. 后端配置与模型

- [x] 1.1 将触发词扫描默认模式归一为 `precise`，并确保新建扫描配置不再默认启用混合模式
- [x] 1.2 更新 `TriggerScanConfig` 与请求校验：显式 `scan_mode=hybrid` 返回可理解错误，省略模式时使用精扫
- [x] 1.3 从活跃扫描默认配置中移除 `coarse_summary_batch_size`，并兼容读取旧配置文件中的遗留字段
- [x] 1.4 保留报告与历史结果中的 `scan_mode` 字段，但新报告固定写入 `precise`

## 2. 后端扫描流程

- [x] 2.1 删除启动前检查中对小总结覆盖率的要求和相关决策项
- [x] 2.2 删除扫描编排中的粗筛阶段，始终对选定章节构建原文精扫批次
- [x] 2.3 更新实时进度事件，移除粗筛阶段事件，仅保留精扫、验证、聚合和报告写入
- [x] 2.4 保留并验证二次验证、低置信度保留、跳过建议和聚合逻辑在单一精扫路径下继续工作

## 3. 提示词工作流

- [x] 3.1 从默认提示词定义中移除 `trigger_coarse_scan` 活跃节点
- [x] 3.2 更新触发词扫描工作流节点列表，只暴露精扫、验证和聚合节点
- [x] 3.3 更新提示词渲染与变量校验测试，确认不再要求粗筛变量

## 4. 前端工作台

- [x] 4.1 更新触发词扫描类型定义，去掉可选的混合模式入口和粗筛批量活跃字段
- [x] 4.2 移除触发词扫描页面的扫描模式选择器与 `coarse_summary_batch_size` 控件
- [x] 4.3 更新启动前检查弹窗与决策处理，删除生成缺失小总结、仅扫描已覆盖章节、切换到精扫等混合模式决策
- [x] 4.4 确保页面仍可配置扫描范围、扫描 API、`precise_chapter_batch_size`、二次验证、验证 API 和报告相关选项

## 5. 测试与文档

- [x] 5.1 更新模型与配置测试，覆盖默认精扫、旧 `hybrid` 默认配置迁移、显式混合请求拒绝
- [x] 5.2 更新扫描流程测试，覆盖无小总结时仍可启动、所选章节全部进入精扫批次、无粗筛事件
- [x] 5.3 更新前端或 API 测试，覆盖启动 payload 不再依赖混合模式字段
- [x] 5.4 更新触发词扫描相关文档，移除混合模式说明并说明通过 `precise_chapter_batch_size` 控制批量
- [x] 5.5 运行最小相关测试集，并记录无法运行的检查及原因
