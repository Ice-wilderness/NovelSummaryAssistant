## 1. Decision Gates

- [x] 1.1 WebUI 技术路线确认：采用 FastAPI + React/Vite/TypeScript。
- [x] 1.2 旧 CustomTkinter GUI 策略确认：WebUI 稳定前过渡保留，WebUI 稳定后移除。
- [x] 1.3 逻辑优化范围确认：本次迁移同时处理已发现的结构和逻辑问题，但按小任务拆分验证。
- [x] 1.4 API Key 保存策略确认：采用本地配置文件 + 环境变量覆盖。
- [x] 1.5 项目署名确认：原作者 `zhoufei_1314`，现作者 `Ice_wilderness`。

## 2. Baseline Audit

- [x] 2.1 运行当前 `run_gui.py` 启动检查，记录启动失败或导入失败信息。
- [x] 2.2 运行核心模块导入检查，覆盖 `gui`、`logic`、`splitters` 目录。
- [x] 2.3 记录当前依赖清单和实际使用点，区分保留依赖、WebUI 新依赖和迁移后可删除依赖。
- [x] 2.4 梳理现有用户数据位置：`api_configs.json`、`config.yaml`、`prompt_cache`、`.summarizer_cache`。
- [x] 2.5 梳理现有任务入口：小说总结、文章总结、自定义总结、章节分割、获取模型。
- [x] 2.6 梳理现有输出目录和文件命名规则，避免 WebUI 迁移改变用户已有产出。

## 3. Package And Import Cleanup

- [x] 3.1 确定最终 Python 包布局，避免继续依赖不存在的 `python.*` 历史包路径。
- [x] 3.2 添加必要的 `__init__.py` 或调整入口路径，使 `gui`、`logic`、`splitters` 可稳定导入。
- [x] 3.3 批量替换 `python.logic.*` 导入为项目实际包路径。
- [x] 3.4 批量替换 `python.gui.*` 导入为项目实际包路径。
- [x] 3.5 修正文件头注释中误导性的历史路径描述。
- [x] 3.6 增加最小导入测试，确保核心模块在不启动 UI 的情况下可导入。

## 4. Logic Bug Fixes Before WebUI Binding

- [x] 4.1 修复 `article_summary_logic.py` 中不存在的状态读写函数调用。
- [x] 4.2 修复 `article_summary_logic.py` 与 `get_llm_summary_with_config` 的参数签名不一致问题。
- [x] 4.3 明确文章总结流程的同步/异步入口，只保留一种对 Web 后端友好的调用方式。
- [x] 4.4 检查小说总结流程中的 `task_id` 参数是否实际使用，移除无效参数或补齐用途。
- [x] 4.5 检查暂停和取消在小说总结、文章总结、自定义总结、拆章任务中的行为差异。
- [x] 4.6 为 LLM API 错误判断补充单元测试，覆盖空响应、HTML 响应、限流和永久失败。

## 5. Configuration Model

- [x] 5.1 定义 API 配置数据模型，包含 `id`、`url`、`key`、`model`、`max_tokens`、`temperature`、`stream`、`timeout`、`max_retries`、`is_active`。
- [x] 5.2 定义提示词配置模型，覆盖默认提示词、用户保存提示词和重置行为。
- [x] 5.3 定义小说字数设置模型，覆盖小结、大结、超级总结、终极总结各字段。
- [x] 5.4 定义文章总结字数设置模型，覆盖段落总结和最终总结字段。
- [x] 5.5 定义任务启动参数模型，分别覆盖小说、文章、自定义总结和章节分割。
- [x] 5.6 实现配置加载服务，兼容现有 `api_configs.json` 和提示词缓存。
- [x] 5.7 实现配置保存服务，保存前统一校验并补齐默认值。
- [x] 5.8 实现 API Key 遮蔽返回，避免普通配置读取接口返回完整密钥。
- [x] 5.9 实现 API Key 环境变量覆盖字段，运行时优先读取环境变量。
- [x] 5.10 实现环境变量缺失时回退到本地配置文件 Key。
- [x] 5.11 增加 API Key 策略测试，覆盖本地 Key、环境变量覆盖、环境变量缺失和遮蔽返回。

## 6. Path, Cache, And File Services

- [x] 6.1 新增路径服务，集中处理项目根目录、运行目录、用户选择目录和打包后路径。
- [x] 6.2 新增缓存服务，集中处理 `.summarizer_cache` 创建、读取、清理和状态文件路径。
- [x] 6.3 新增提示词缓存服务，集中处理 `prompt_cache` 路径和读写。
- [x] 6.4 统一文本读取函数，覆盖 UTF-8、UTF-8 BOM、GBK、GB18030 和 chardet 检测。
- [x] 6.5 统一安全文件名函数，替换重复的文件名清理实现。
- [x] 6.6 统一章节自然排序函数，明确阿拉伯数字、中文数字和英文数字排序规则。
- [x] 6.7 为中文路径、空格路径和长文件名增加测试。

## 7. Task Runtime Service

- [x] 7.1 定义统一任务状态枚举：pending、running、paused、canceling、cancelled、success、failed。
- [x] 7.2 定义任务记录模型，包含任务 ID、类型、参数摘要、状态、进度、创建时间、结束时间、错误信息。
- [x] 7.3 实现任务注册表，支持按任务 ID 查询当前任务。
- [x] 7.4 实现任务事件队列，统一承载日志、进度、状态变化和错误事件。
- [x] 7.5 实现异步任务启动器，替代 GUI 中散落的线程和事件循环管理。
- [x] 7.6 实现暂停/恢复控制，适配现有 `pause_event` 机制。
- [x] 7.7 实现取消控制，确保 `asyncio.Task.cancel()` 后任务能进入终态。
- [x] 7.8 实现任务完成回调，清理任务资源并保留最终结果摘要。
- [x] 7.9 为任务运行时增加单元测试，覆盖启动、查询、暂停、恢复、取消和异常。

## 8. Backend API

- [x] 8.1 新增 FastAPI 应用入口和健康检查接口。
- [x] 8.2 新增配置读取接口。
- [x] 8.3 新增配置保存接口。
- [x] 8.4 新增提示词读取接口。
- [x] 8.5 新增提示词保存和重置接口。
- [x] 8.6 新增模型列表获取接口，复用现有 `fetch_available_models`。
- [x] 8.7 新增小说总结任务启动接口。
- [x] 8.8 新增文章总结任务启动接口。
- [x] 8.9 新增自定义总结任务启动接口。
- [x] 8.10 新增章节分割任务启动接口。
- [x] 8.11 新增任务状态查询接口。
- [x] 8.12 新增任务暂停、恢复、取消接口。
- [x] 8.13 新增 SSE 日志和进度事件流接口。
- [x] 8.14 增加后端接口测试，覆盖成功请求、参数错误、任务不存在和取消请求。

## 9. Workflow Binding

- [x] 9.1 将小说总结流程包装为任务运行时可调用服务，保持断点续跑和多 API 分配行为。
- [x] 9.2 将文章总结流程包装为任务运行时可调用服务，确保修复后的流程可完成两阶段总结。
- [x] 9.3 将自定义总结流程包装为任务运行时可调用服务，返回可展示的文本结果。
- [x] 9.4 将章节分割流程包装为任务运行时可调用服务，返回生成文件数量和输出目录。
- [x] 9.5 将获取模型流程包装为后端接口，避免前端直接持有 API Key。
- [x] 9.6 将日志输出统一转换为结构化事件，保留全局日志和 API 专属日志路由信息。

## 10. Frontend Foundation

- [x] 10.1 创建 React/Vite/TypeScript 前端目录。
- [x] 10.2 配置前端开发脚本和后端代理。
- [x] 10.3 建立 API 客户端封装，统一处理请求、错误和任务事件订阅。
- [x] 10.4 建立应用级状态管理，保存当前配置、任务状态和日志流。
- [x] 10.5 建立基础布局：侧边导航、顶部任务状态、主工作区、底部或侧边日志区。
- [x] 10.6 建立通用表单组件：文本输入、数字输入、开关、选择器、路径输入、文件列表。
- [x] 10.7 建立通用日志组件：全局日志、API 日志标签、状态颜色和自动滚动。

## 11. Frontend Workflow Pages

- [ ] 11.1 实现小说总结页面的基础表单和校验。
- [ ] 11.2 实现小说总结页面的字数设置编辑。
- [ ] 11.3 实现小说总结页面的精细控制和终极总结 API 选择。
- [ ] 11.4 实现文章总结页面的文件选择、输出子目录和字数设置。
- [ ] 11.5 实现自定义总结页面的多文件选择、自定义指令和结果展示。
- [ ] 11.6 实现章节分割页面的默认模式。
- [ ] 11.7 实现章节分割页面的正则模式和分卷处理。
- [ ] 11.8 实现章节分割页面的标题列表模式。
- [ ] 11.9 实现提示词编辑页面的加载、保存、重置和变更提示。
- [ ] 11.10 实现 API 配置页面的新增、删除、启用、保存和加载。
- [ ] 11.11 实现 API 配置页面的密钥遮蔽、显示切换和获取模型。
- [ ] 11.12 实现任务控制按钮，按任务状态启用或禁用启动、暂停、恢复、取消。
- [ ] 11.13 实现 WebUI 署名展示，显示原作者 `zhoufei_1314` 和现作者 `Ice_wilderness`。

## 12. UI Polish And Responsiveness

- [ ] 12.1 建立统一视觉主题，控制颜色、间距、字号、按钮、表格和日志样式。
- [ ] 12.2 优化表单密度，让长配置页面适合反复使用。
- [ ] 12.3 优化任务运行中状态反馈，保证用户能看到当前阶段、API 和最新日志。
- [ ] 12.4 验证桌面宽度下各页面无文字溢出和控件重叠。
- [ ] 12.5 验证常见平板宽度下导航、表单和日志仍可使用。
- [ ] 12.6 为错误、空状态、加载状态和任务完成状态补齐 UI。

## 13. Legacy GUI Removal After WebUI Stable

- [ ] 13.1 将 `run_gui.py` 改为过渡入口，优先启动或提示 WebUI。
- [ ] 13.2 WebUI 冒烟测试通过前保留 `gui/` 目录。
- [ ] 13.3 WebUI 稳定后移除 CustomTkinter GUI 代码。
- [ ] 13.4 WebUI 稳定后从依赖中移除 `customtkinter`。
- [ ] 13.5 更新文档，删除旧 GUI 使用说明，保留必要的迁移说明。

## 14. Verification And Documentation

- [ ] 14.1 运行 Python 单元测试。
- [ ] 14.2 运行后端接口测试。
- [ ] 14.3 运行前端类型检查和构建。
- [ ] 14.4 用小型样例执行章节分割端到端检查。
- [ ] 14.5 用小型样例执行至少一个总结任务端到端检查。
- [ ] 14.6 验证任务暂停、恢复和取消。
- [ ] 14.7 验证已有配置和缓存迁移兼容。
- [ ] 14.8 验证 API Key 不出现在普通配置响应、前端日志和任务日志中。
- [ ] 14.9 验证环境变量覆盖 API Key 的行为。
- [ ] 14.10 验证 WebUI 和文档中正确展示原作者与现作者署名。
- [ ] 14.11 更新 README 或启动说明，覆盖 WebUI 启动、开发模式、构建模式、配置位置、API Key 策略、项目署名和已知限制。
